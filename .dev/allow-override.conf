# Permite que Apache lea el .htaccess del proyecto (rewrite + ErrorDocument).
# XAMPP ya trae esto de fábrica; la imagen php:apache no.
<Directory /var/www/html>
    AllowOverride All
    Require all granted
</Directory>
