# Imagen de desarrollo: Apache + PHP con mod_rewrite habilitado.
# Sin dependencias externas: la extracción usa solo el núcleo de PHP
# (Phar/PharData para tar, y zlib para zip/gz).
FROM php:8.3-apache
RUN a2enmod rewrite
